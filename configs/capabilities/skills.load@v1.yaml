capability_name: skills.load@v1
description: Load skill content and referenced files
adapter_type: local
side_effect_level: none
requires_approval_default: false
timeout_ms: 30000
input_schema:
  type: object
  properties:
    skill_id:
      type: string
    include:
      type: array
      items:
        type: string
  required:
    - skill_id
output_schema:
  type: object
  additionalProperties: true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentkernel-example-tool-workflow"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["agentkernel>=0.1.0"]
